# 건강 프로필 → 질문 → 통화 기록 → 리포트 설계

마지막 갱신: 2026-08-13

## 1. 제품 중심축

콜록의 주 기능은 기침 분류기가 아니다. 가족이 미리 등록하고 본인이 확인한 건강 맥락을
바탕으로 다음 통화에서 확인할 질문을 제시하고, 실제 통화에서 당사자가 말한 답을 근거와 함께
구조화해 주간·월간 변화 기록으로 돌려주는 것이다.

```text
본인 확인 건강 프로필
  └─ 질환 · 복용약 · 걱정 항목
       ↓
질문 정책
  └─ 검수된 질문 선택 · 최근 반복 회피 · 이전 답 후속 확인
       ↓
통화
  └─ 질문 미리보기/ElevenLabs TTS · 양 화자 분리 STT
       ↓
건강 관찰 이벤트
  └─ 당사자 답변 · 질문/답변 근거 · 시점 · 부정/불확실성
       ↓
주간·월간 리포트
  └─ 자기보고 변화 · 음향 보조 관찰 · 다음 확인 질문
```

음향 지표는 이 흐름을 보조한다. 기침 의심 구간, 발화 속도, 휴지, 대화 수리는 사용자가
말한 건강 사실을 대체하거나 질환을 판정하지 않는다.

## 2. 정보 종류를 섞지 않는다

| 종류 | 예 | 의미 | 화면 표시 |
|---|---|---|---|
| 프로필 사실 | 고혈압, 복용 중인 혈압약 | 사용자가 입력하고 당사자가 확인한 비교적 안정적인 맥락 | `프로필에 등록됨` |
| 통화 자기보고 | 어젯밤 두 번 깼음, 오늘 약을 못 먹음 | 특정 통화에서 당사자가 직접 말한 시점성 사실 | `이번 통화에서 언급` |
| 음향 관찰 | 분석 가능 12분 중 기침 의심 3건 | 모델이 소리에서 관찰한 보조값 | `음향에서 관찰`, detector 버전/한계 병기 |
| 시스템 추론 | 3주 연속 수면 불편 언급 증가 | 규칙으로 여러 관찰을 집계한 변화 | 비교 기간과 근거 통화 수 표시 |

리포트가 위 네 종류를 한 문장에 섞으면 사용자가 입력한 질환을 AI가 새로 발견한 것처럼
보이거나, 음향 후보가 진단처럼 보인다. API와 UI 모두 출처를 보존해야 한다.

## 3. 건강 프로필 계약

새 화면은 부모와 자녀 모두 자기 건강 프로필을 갖고 상대에게 전화할 수 있음을 전제로 한다.
따라서 현재 `ParentProfile(parent_id, conditions)`보다 일반적인 아래 구조가 필요하다.

### 3-1. 권장 엔터티

- `HealthProfile`: `subjectUserId`, 생년월일/성별(선택), 갱신 시각
- `HealthCondition`: 표준 코드, 사용자 표시명, 상태(active/resolved/concern), 시작 시점(선택)
- `Medication`: 약 이름, 용량(선택), 복용 주기(선택), 복용 목적(선택), active 여부
- `HealthConcern`: 아직 진단받지 않은 걱정 항목. 질환과 구분한다.
- 모든 항목 공통: `source`(`SELF`, `FAMILY_PROPOSED`), `confirmedBySubjectAt`, `updatedAt`

부모/자녀라는 역할보다 `subjectUserId`가 건강 데이터의 주체다. 가족이 먼저 입력한 값은
확정 프로필이 아니라 `FAMILY_PROPOSED`이며, 당사자가 초대 수락 화면에서 확인하거나 수정한
뒤에만 질문과 리포트의 확정 맥락으로 사용한다. 당사자의 값이 우선하며 가족이 조용히
덮어쓸 수 없게 한다.

초대 수락 전 제3자의 질환·복용약을 서버에 저장하는 UX는 개인정보·동의 위험이 있다. 가장
안전한 MVP 순서는 `초대 → 당사자 동의 → 당사자 입력 또는 가족 제안 확인`이다. 피그마의
선입력을 유지해야 한다면 서버 확정 데이터로 취급하지 않는 별도 draft 정책을 먼저 결정한다.

## 4. 통화와 건강 주체

현재 구현은 `CallRecord(parent_id, child_id)`와 `POST /calls`의 CHILD-only 검사로 자녀 발신,
부모 분석에 고정돼 있다. 새 화면처럼 양쪽이 서로 전화하려면 다음 세 식별자를 분리한다.

- `callerUserId`: 전화를 건 사람
- `calleeUserId`: 전화를 받은 사람
- `subjectUserId`: 이번 질문과 분석이 다루는 한 사람

권장 규칙은 홈에서 선택한 가족 구성원, 즉 통화 상대를 이번 통화의 `subjectUserId`로 두는
것이다. 그러면 자녀가 부모에게 걸면 부모가, 부모가 자녀에게 걸면 자녀가 건강 주체가 된다.
한 통화에서 두 사람을 동시에 분석하면 질문·동의·리포트 소유권이 모호해지므로 MVP에서는
한 통화당 한 명으로 제한한다.

이 변경 시 STT/LLM도 `PARENT`라는 고정 화자명이 아니라 `SUBJECT`와 `FAMILY`를 사용하고,
PCM/음향 분석은 subject track에만 수행해야 한다. subject의 유효한 분석 동의가 없으면 통화는
가능하되 녹음·전사·분석은 전부 비활성화한다.

## 5. 질문 정책

질문은 LLM이 임의로 의료 문진을 만들어 내는 구조보다 검수된 템플릿을 선택하는 구조가
안전하고 평가 가능하다.

### 입력

- 당사자가 확인한 active 질환·복용약·걱정 항목
- 최근 통화의 unresolved/changed 자기보고 이벤트
- 최근 물어본 question ID와 빈도
- 통화 시간대와 질문 난이도/anchor group

### 출력

- `questionId`, `subjectUserId`, `topic`, `text`, `selectionReason`
- `anchorGroup`, `templateVersion`, `profileItemIds`
- ElevenLabs TTS asset URL과 장애 시 iOS local fallback

### 선택 원칙

1. 질환·복약에 연결된 질문 한 개, 생활/일반 상태 질문 한 개를 우선한다.
2. 동일 질문의 과도한 반복은 피하되 비교용 anchor 질문은 동등한 난이도로 유지한다.
3. 진단명 유도, 원인 추정, 치료 변경 질문은 만들지 않는다.
4. 짧은 `응/아니`만 나오지 않도록 열린 질문을 우선한다.

예: `어젯밤 잠은 어땠어요?`, `오늘 약은 언제 챙겨 드셨어요?`, `오늘 밖에서는 어떻게
움직이셨어요?`. `약 드셨어요?`처럼 예/아니오만 요구하면 현재 grounding validator가 일반
응답을 사실로 만들지 않기 때문에 유용한 기록이 남지 않을 수 있다.

Gemini는 실시간 통화 경로에 둘 필요가 없다. 프로필이 바뀌거나 하루 질문을 준비할 때
템플릿을 순위화하고, 통화 후 사실 추출과 리포트 문장화에 사용한다. 결과는 캐시할 수 있어
레이턴시보다 근거성·재현성이 중요하다.

## 6. STT와 구조화 추출

Deepgram 전사는 기록의 입력이지 건강 사실 그 자체가 아니다. 통화 후 아래 순서로 처리한다.

1. 양 화자 track을 각각 STT하고 시간축으로 합친다.
2. subject 발화와 직전 가족 질문을 묶어 question-answer pair를 만든다.
3. Gemini가 `symptom`, `medication`, `activity`, `sleep`, 이후 `function/nutrition` 범주를
   구조화한다.
4. 각 fact에 `subjectUserId`, `questionId`, `polarity`, `evidenceSegmentIds`, `observedAt`,
   `schemaVersion`을 저장한다.
5. 존재하지 않는 segment, 다른 화자의 추측, 진단·원인·치료 문장은 validator가 제거한다.

현재 prompt는 안전을 위해 `응/아니/괜찮아`만으로 가족의 질문 내용을 사실화하지 않는다.
실사용에서 짧은 답을 지원하려면 단순히 이 제한을 풀지 말고, 실제로 인접한 질문 segment와
subject 답 segment를 함께 근거로 참조하는 Q/A schema 및 eval fixture를 먼저 추가한다.

개인정보 안내와 맞추려면 원본 오디오뿐 아니라 전체 전사 보관 여부도 명시해야 한다. 현재
구현은 성공한 통화의 전체 segment 전사를 DB에 저장한다. 화면 문구가 `구조화된 대화 항목만
기록`이라면 장기 보관은 건강 fact와 필요한 최소 evidence snippet으로 제한하고 전체 전사의
보관 기간/열람 범위를 별도로 정해야 한다.

## 7. 주간·월간 리포트

리포트는 LLM 자유 요약 하나가 아니라 먼저 결정적 집계를 만들고, 마지막 문장만 LLM으로
다듬는다.

- 기간, 분석된 통화 수/분, 제외된 통화 수
- 주제별 PRESENT/ABSENT/UNCERTAIN 이벤트와 근거 통화
- 이전 주/월 대비 새로 생김, 지속, 해소, 데이터 부족
- 개인 기준선 대비 검증된 음향 관찰
- 다음 통화에서 확인할 질문과 선택 이유
- 의료 진단이 아니라는 고지

피그마의 `기침 횟수가 5번으로 측정되어 의료진 상담을 권유해요`는 detector가 검증되지 않은
현재 상태에서 과도하다. MVP 문구는 `이번 통화의 분석 가능한 N분에서 기침 의심 구간 M건이
관찰됐어요`처럼 관찰과 분모를 표시한다. 전문가 상담 문구는 불편 호소, 지속 기간, 사용자가
등록한 대응 규칙 등 별도 조건이 있을 때만 일반적 안내로 분리한다.

## 8. 현재 코드와의 차이

| 항목 | 현재 구현 | 필요한 변경 |
|---|---|---|
| 프로필 | 부모별 질환 코드 배열 | 모든 사용자 subject 프로필, 약/걱정/출처/본인 확인 |
| 초대 전 입력 | 동의 전 프로필 저장 차단 | 현재 안전 규칙 유지 또는 별도 draft 정책 결정 |
| 통화 방향 | CHILD → PARENT 고정 | caller/callee/subject 분리, 양방향 권한 |
| 질문 | 질환별 정적 pool, 하루 2개 | 약·걱정·이전 답 반영, selection reason/anchor version |
| 추출 | 부모-only 4범주와 parent evidence | subject 중심 Q/A evidence, 기능/영양 확장 검토 |
| 전사 보관 | 전체 segment JSON 저장 | 동의 문구와 보관 정책 정합화 |
| 리포트 | 범주별 문자열 배열+음향 points | typed event 변화, 출처/근거/데이터 충분성 표시 |
| 음향 | 4종 prototype | content report의 보조 레이어, 미검증값 숨김 |

## 9. 구현 순서

1. `subject`가 누구인지와 한 통화 한 subject 원칙을 제품 계약으로 확정한다.
2. 일반화한 프로필/질환/복용약/걱정 데이터 모델과 본인 확인 상태를 추가한다.
3. 초대·동의·프로필 작성 순서를 확정하고 API를 피그마 흐름에 맞춘다.
4. 질문 template metadata와 deterministic selector를 확장한다.
5. Q/A evidence 기반 추출 schema/eval을 추가한다.
6. 건강 observation event 기반 주·월간 report v2를 구현한다.
7. 마지막에 검증된 음향 지표만 보조 카드로 연결한다.

HealthKit은 이 중심 흐름의 필수 요건이 아니다. 나중에 사용자가 별도 권한을 허용할 때 활동·
수면 추세를 보강하는 선택 기능으로 두며, 해커톤 MVP는 프로필·질문·통화 자기보고·리포트의
폐쇄 루프를 먼저 완성한다.
